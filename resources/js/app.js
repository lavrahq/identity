const Turbolinks = require('turbolinks');

/**
 * Launch Turbolinks, a JavaScript library that hijacks all links, allowing them to be downloaded
 * and then rendered over the existing webpage for faster SPA-like rendering.
 */
Turbolinks.start();
