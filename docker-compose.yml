#
# FOR DEVELOPMENT ONLY
#
# This compose file is suitable for development only.
# For production, please use the prebuild image.

version: '3.7'
services:
  gql:
    container_name: identity_gql
    image: hasura/graphql-engine:v1.0.0-beta.2.cli-migrations
    ports:
      - "9001:9001"
    restart: unless-stopped
    env_file:
      - ./.env
    environment:
      HASURA_GRAPHQL_SERVER_PORT: 9001
      HASURA_GRAPHQL_ENABLE_CONSOLE: "false"
      HASURA_GRAPHQL_AUTH_HOOK: null
      HASURA_GRAPHQL_AUTH_HOOK_MODE: POST
      HASURA_GRAPHQL_ENABLE_TELEMETRY: "true"
      HASURA_GRAPHQL_PG_STRIPES: 1
      HASURA_GRAPHQL_PG_CONNECTIONS: 50
      HASURA_GRAPHQL_PG_TIMEOUT: 180
      HASURA_GRAPHQL_USE_PREPARED_STATEMENTS: "true"
      HASURA_GRAPHQL_TX_ISOLATION: "read-committed"
      HASURA_GRAPHQL_ENABLED_APIS: metadata,graphql,pgdump
      HASURA_GRAPHQL_STRINGIFY_NUMERIC_TYPES: "false"
      HASURA_GRAPHQL_LIVE_QUERIES_FALLBACK_REFETCH_INTERVAL: 1000
      HASURA_GRAPHQL_LIVE_QUERIES_MULTIPLEXED_REFETCH_INTERVAL: 1000
      HASURA_GRAPHQL_LIVE_QUERIES_MULTIPLEXED_BATCH_SIZE: 100
      HASURA_GRAPHQL_ENABLE_ALLOWLIST: "false"
    volumes:
      - ./graphql/migrations:/hasura-migrations
  # api:
  #   container_name: identity_api
  #   image: richarvey/nginx-php-fpm:1.7.1
  #   ports:
  #     - "9002:80"
  #   restart: unless-stopped
  #   env_file:
  #     - ./.env
  #   environment:
  #     WEBROOT: /var/www/html/public
  #   volumes:
  #     - ./:/var/www/html
